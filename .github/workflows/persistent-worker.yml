name: Persistent Worker

on:
  repository_dispatch:
    types: [start-worker]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: Create Temp Requirements for Caching
        run: |
          echo -e "gradio_client\nrequests[socks]\npillow\nhttpx[socks]\npysocks\nsocksio" > temp_requirements.txt

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: temp_requirements.txt

      - name: Install Base Dependencies
        run: |
          pip install -r temp_requirements.txt

      - name: Cache WARP Docker Image
        id: cache-warp
        uses: actions/cache@v4
        with:
          path: ~/warp-image.tar
          key: ${{ runner.os }}-warp-image-v1

      - name: Load WARP Docker Image from Cache
        if: steps.cache-warp.outputs.cache-hit == 'true'
        run: docker load -i ~/warp-image.tar

      - name: Pull and Save WARP Docker Image
        if: steps.cache-warp.outputs.cache-hit != 'true'
        run: |
          docker pull caomingjun/warp
          docker save -o ~/warp-image.tar caomingjun/warp

      - name: Start 20 Parallel WARP Containers
        run: |
          # دانلود و اجرای سریع ۲۰ کانتینر داکر وارپ روی پورت‌های ۴۰۰۱ تا ۴۰۲۰ به صورت پله‌ای
          for i in {1..20}; do
            PORT=$((4000 + i))
            docker run -d --name warp$i --cap-add=NET_ADMIN -p $PORT:1080 caomingjun/warp
            sleep 0.5
          done
          
          # زمان ثبات اولیه برای لود شدن کارت شبکه‌ها
          sleep 12
          
          # تست پورت‌های اول و دوم جهت تایید نهایی تغییر آی‌پی‌ها
          curl --socks5-hostname 127.0.0.1:4001 https://ifconfig.me || true
          curl --socks5-hostname 127.0.0.1:4002 https://ifconfig.me || true

      - name: Run Persistent Worker
        env:
          SPACE_URL: ${{ github.event.client_payload.space_url }}
        run: |
          cat << 'EOF' > worker.py
          import os
          import sys
          import time
          import json
          import socket
          import requests
          import subprocess
          import threading
          import random
          from datetime import datetime

          SPACE_URL = os.environ.get("SPACE_URL")
          if not SPACE_URL:
              print("Error: SPACE_URL env var is not set.", flush=True)
              sys.exit(1)

          print(f"Starting long-running multi-worker with 20 WARPs for Space: {SPACE_URL}", flush=True)

          start_time = time.time()
          max_run_duration = 5 * 60 * 60  # 5 hours
          github_run_id = os.environ.get("GITHUB_RUN_ID", "local-worker")

          # تشخیص آی‌پی مستقیم هاست برای جلوگیری از تداخل فلو وارپ
          HOST_IP = "Unknown"
          try:
              HOST_IP = requests.get("https://api.ipify.org", timeout=5).text.strip()
              print(f"Host direct IP detected: {HOST_IP}", flush=True)
          except Exception as e:
              print(f"Failed to detect Host IP: {e}", flush=True)

          def check_and_wait_for_warp(socks_port):
              """صبر کردن برای فعال شدن کامل کانتینر داکر وارپ پس از ریست"""
              for _ in range(15):
                  try:
                      proxies = {
                          'http': f'socks5h://127.0.0.1:{socks_port}',
                          'https': f'socks5h://127.0.0.1:{socks_port}'
                      }
                      r = requests.get("https://api.ipify.org", proxies=proxies, timeout=3)
                      if r.status_code == 200:
                          ip = r.text.strip()
                          # اگر آی‌پی برابر آی‌پی مستقیم هاست بود یعنی وارپ هنوز وصل نشده و ترافیک لوکال رد می‌شود
                          if ip != HOST_IP and ip != "Unknown":
                              return ip
                  except:
                      pass
                  time.sleep(1)
              return None

          def renew_warp_ip(worker_index, socks_port):
              """بازسازی کامل کانتینر داکر و شکستن چسبندگی آی‌پی با سوییچ انی‌کست"""
              try:
                  container_name = f"warp{worker_index}"
                  
                  # ۱. حذف کامل کانتینر خراب/قدیمی
                  subprocess.run(f"docker rm -f {container_name}", shell=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
                  time.sleep(1.5) # زمان کافی برای قطع اتصال فیزیکی آژور
                  
                  # ۲. ساخت کانتینر کاملاً تمیز و تازه نفس
                  subprocess.run(f"docker run -d --name {container_name} --cap-add=NET_ADMIN -p {socks_port}:1080 caomingjun/warp", shell=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
                  time.sleep(3) # زمان کوتاه برای لود شدن سرویس وارپ داخل کانتینر
                  
                  # ۳. سوییچ کردن انی‌کست مقصد برای تغییر قطعی رنج آی‌پی
                  endpoints = [
                      "162.159.192.1:2408",
                      "162.159.193.1:2408",
                      "162.159.194.1:2408",
                      "162.159.195.1:2408",
                      "162.159.204.1:2408"
                  ]
                  selected_endpoint = random.choice(endpoints)
                  subprocess.run(f"docker exec {container_name} warp-cli --accept-tos tunnel endpoint set {selected_endpoint}", shell=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
                  
                  # ۴. اتصال مجدد
                  subprocess.run(f"docker exec {container_name} warp-cli --accept-tos connect", shell=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
                  
                  # ۵. انتظار برای دریافت آی‌پی جدید
                  current_ip = check_and_wait_for_warp(socks_port)
                  if current_ip:
                      print(f"🔄 [Worker {worker_index}] Fresh WARP IP established: {current_ip} (Endpoint: {selected_endpoint})", flush=True)
                      return current_ip
              except Exception as e:
                  print(f"⚠️ [Worker {worker_index}] Error in secure renew: {e}", flush=True)
              return None

          def run_worker_thread(worker_index, socks_port):
              print(f"[{worker_index}] Worker thread started using SOCKS {socks_port}", flush=True)
              thread_worker_id = f"{github_run_id}_w{worker_index}"
              
              while time.time() - start_time < max_run_duration:
                  try:
                      # ارسال ضربان قلب مجهز به شناسه منحصربه‌فرد این نخ کارگر موازی
                      try:
                          requests.post(f"{SPACE_URL}/api/worker/heartbeat", json={"worker_id": thread_worker_id}, timeout=10)
                      except Exception as e:
                          pass

                      # Poll for new jobs
                      r = requests.post(f"{SPACE_URL}/api/worker/poll", json={}, timeout=15)
                      if r.status_code == 200:
                          data = r.json()
                          if data.get("status") == "success":
                              run_id = data.get("run_id")
                              event_type = data.get("event_type")
                              client_payload = data.get("status") if 'client_payload' not in data else data.get("client_payload", {})
                              
                              if not client_payload or client_payload == "success":
                                  client_payload = data.get("client_payload", {})

                              print(f"[{run_id}] Thread {worker_index} grabbed job ({event_type})", flush=True)
                              
                              # تنظیمات محیطی استاندارد کارگر
                              job_env = os.environ.copy()
                              
                              # ارجاع ترافیک پردازش این کارگر به پورت پروکسی وارپ اختصاصی خودش
                              job_env["HTTP_PROXY"] = f"socks5h://127.0.0.1:{socks_port}"
                              job_env["HTTPS_PROXY"] = f"socks5h://127.0.0.1:{socks_port}"
                              
                              # Map client_payload to environment variables
                              for key, val in client_payload.items():
                                  if key != "data":
                                      job_env[key.upper()] = str(val)
                              
                              payload_data = client_payload.get("data", {})
                              if isinstance(payload_data, dict):
                                  for key, val in payload_data.items():
                                      if key == "image_urls":
                                          job_env["IMAGE_URLS"] = json.dumps(val)
                                      else:
                                          job_env[key.upper()] = str(val)
                          
                              # Extract the script
                              script_content = payload_data.get("script") or client_payload.get("script")
                              if not script_content:
                                  print(f"Error: No script found in payload for job {run_id}", flush=True)
                                  continue
                                  
                              # ایجاد فایل اسکریپت منحصربه‌فرد برای این کارگر جهت عدم تداخل با بقیه نخ‌ها
                              script_file = f"run_job_{run_id}_w{worker_index}.py"
                              with open(script_file, "w", encoding="utf-8") as f:
                                  f.write(script_content)
                                  
                              max_ip_attempts = 15
                              ip_attempt = 0
                              job_success = False
                              last_ip = None

                              while ip_attempt < max_ip_attempts:
                                  ip_attempt += 1
                                  current_ip = check_and_wait_for_warp(socks_port) or "Unknown"
                                  
                                  # تایید تغییر واقعی آدرس آی‌پی برای بهینه‌سازی پردازش
                                  if last_ip and current_ip == last_ip and ip_attempt > 1:
                                      print(f"[{run_id}] IP did not change ({current_ip}). Re-requesting WARP container reset...", flush=True)
                                      renew_warp_ip(worker_index, socks_port)
                                      current_ip = check_and_wait_for_warp(socks_port) or "Unknown"
                                      
                                  last_ip = current_ip
                                  print(f"[{run_id}] Current Cloudflare WARP IP: {current_ip}", flush=True)
                                  print(f"Executing script for job {run_id} (IP Attempt {ip_attempt}/{max_ip_attempts})...", flush=True)
                                  
                                  process = subprocess.Popen(
                                      [sys.executable, script_file],
                                      env=job_env,
                                      stdout=subprocess.PIPE,
                                      stderr=subprocess.STDOUT,
                                      text=True
                                  )
                                  
                                  output_lines = []
                                  for line in process.stdout:
                                      print(f"[{run_id}] {line.strip()}", flush=True)
                                      output_lines.append(line)
                                      
                                  process.wait()
                                  
                                  if process.returncode == 0:
                                      print(f"Job {run_id} finished successfully.", flush=True)
                                      job_success = True
                                      break
                                  
                                  # بروز خطا در فرآیند انتشار یا دریافت خروجی (خطای خروجی غیر از صفر)
                                  print(f"[{run_id}] ⚠️ Job failed (Exit Code {process.returncode}). Assuming IP rate-limit/quota issue.", flush=True)
                                  if ip_attempt < max_ip_attempts:
                                      print(f"[{run_id}] Rotating Cloudflare WARP container and retrying...", flush=True)
                                      renew_warp_ip(worker_index, socks_port)
                                      time.sleep(2)
                                      continue
                                  else:
                                      print(f"[{run_id}] Max IP rotation attempts reached for this job.", flush=True)
                                      break
                                      
                              try:
                                  os.remove(script_file)
                              except:
                                  pass

                              if not job_success:
                                  requests.post(
                                      f"{SPACE_URL}/api/webhook/fail",
                                      json={
                                          "run_id": run_id,
                                          "error": "All Cloudflare WARP IP rotation attempts failed due to quota/rate-limit blocks.",
                                          "github_run_id": os.environ.get("GITHUB_RUN_ID", "")
                                      },
                                      timeout=15
                                  )
                                  
                              continue
                              
                  except Exception as e:
                      print(f"[{worker_index}] Error in thread loop: {e}", flush=True)
                      
                  time.sleep(5)

          # راه‌اندازی ۲۰ نخ کارگر موازی به همراه اختصاص پورت‌های داکر مجزا
          threads = []
          for idx in range(1, 21):
              port = 4000 + idx
              t = threading.Thread(target=run_worker_thread, args=(idx, port), daemon=True)
              t.start()
              threads.append(t)

          # نگه داشتن نخ اصلی تا زمانی که زمان اجرا به پایان برسد
          while time.time() - start_time < max_run_duration:
              time.sleep(10)

          print("All worker threads finished. Exiting.", flush=True)
          EOF
          python worker.py
